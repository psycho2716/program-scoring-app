{
  "name": "program-scoring-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/lan-info.mjs && concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "node scripts/lan-info.mjs && npm run dev --prefix frontend",
    "lan:firewall": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/open-lan-firewall.ps1",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
